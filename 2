#include <iostream>
using namespace std;
int main()
{
    int n, i, j, t, k, l, L, buf;
    cin>>n;
    cout<<"L=";
    cin>>L;
    int a[n], b[n], c[n];
    setlocale(LC_ALL, " ");
    t=l=k=0;
    for (i=0; i<n; i++)
    {
        cin>>a[i];
    }
    for (i=l; i<n; i++)
    {
        if (a[i]==a[i+1])
        {
            t+=1;
        } else
        {
            l=t;
            buf=a[i];
            l+=1;
            b[k]=l;
            c[k]=buf;
            k+=1;
            t=0;
        }
    }
    for (i=0; i<k; i++)
    {
        if (b[i]>L)
        {
            c[i]=0;
        }
    }
    for (int i = 0; i < k; i++)
    {
        cout<<c[i]<<" ";
    }
    return 0;
}
